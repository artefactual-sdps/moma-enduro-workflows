apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: moma-enduro-worker
  labels:
    app: moma-enduro-worker
spec:
  serviceName: moma-enduro-worker
  selector:
    matchLabels:
      app: moma-enduro-worker
  template:
    metadata:
      labels:
        app: moma-enduro-worker
    spec:
      serviceAccountName: sdps
      securityContext:
        fsGroup: 1000
      containers:
        - name: moma-enduro-worker
          image: moma-enduro-worker:dev
          volumeMounts:
            - name: config
              mountPath: /home/enduro/.config
              readOnly: true
            - name: shared-dir
              mountPath: /home/enduro/shared
          resources: {}
      volumes:
        - name: config
          secret:
            secretName: moma-enduro-secret
        - name: shared-dir
          persistentVolumeClaim:
            claimName: preprocessing-pvc
